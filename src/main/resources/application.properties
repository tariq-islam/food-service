#
# [ Database Configuration Section ]
#
#spring.jpa.database=POSTGRESQL
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=create-drop

spring.datasource.platform=teiid
spring.database.driverClassName=org.teiid.jdbc.TeiidDriver
# The database connection string is read from an environment variable named 'DATABASE_FQDN'
# The database service resides on port 5432 (Postgres Default) and the database used is 'testdb'
spring.datasource.url=jdbc:teiid:WINEDB@mm://172.30.0.211:31000/${DATABASE_NAME}
# The database username is read from an environment variable
spring.datasource.username=teiidUser
# The database password is read from an environment variable 
spring.datasource.password=redhat1!
#
# [ Other Configuration Attributes ]
#
